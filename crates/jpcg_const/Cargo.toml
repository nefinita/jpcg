[package]
name = "jpcg_const"
license.workspace = true
readme.workspace = true
version.workspace = true
edition.workspace = true

# 常量子系统：drug/food/player 常量。
# 双产物：rlib（静态）/ cdylib（动态模式被 core dlopen）。

[dependencies]

[lib]
crate-type = ["cdylib", "rlib"]

[lints]
workspace = true